<div class="container edit-container">
  <h2 class="heading">Alpha Categories</h1>
  <div>
    <% @categories.each do |category| %>
      <ul class="listing">
        <li>
          <%= link_to "#{category.name}", category_path(category) %>
        </li>
      </ul>
    <% end %>
  </div> 
</div>